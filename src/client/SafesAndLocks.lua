local module = {}
local client = script.Parent
local cameraService = require(client.Camera)

function module:Enter3DSpace(cameraCFrame : CFrame, ObjectToCall : Instance?)
    
end

function module:Exit3DSpace()
    
end

return module